const {v4:uuid} = require('uuid');
const { config, createMessageStoreWithWriteSink, reset } = require('../../integration-test-helpers');

describe('it', () => {
  it('should', () => {
    expect(1).toEqual(1)
  });
});